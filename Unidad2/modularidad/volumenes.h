//evita que el mismo archivo se incluya varias veces en el programa
#ifndef VOLUMENES_H
//evita errores de compilación (ya fue incluido, si lo incluyen de nuevo lo ignoran)
#define VOLUMENES_H

float volumenEsfera(float radio);
float volumenCubo(float lado);
//cierre
#endif